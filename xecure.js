// Simple validation de sécurité par Xecure (peut être étendu)
function xecure(email, password) {
    // Simule une validation de sécurité (ajoute les logiques nécessaires ici)
    return email && password.length >= 8;
}
